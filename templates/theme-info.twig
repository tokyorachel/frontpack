core: 8.x
type: theme
description: {{ description }}

name: {{ name }}

{% if layouts %}
  - {{ machine_name }}/layouts
{% endif %}
regions:
  header: 'Header'
  primary_menu: 'Primary menu'
  secondary_menu: 'Secondary menu'
  page_top: 'Page top'
  page_bottom: 'Page bottom'
  featured: 'Featured'
  breadcrumb: 'Breadcrumb'
  content: 'Content'
  sidebar_first: 'Sidebar first'
  sidebar_second: 'Sidebar second'
  footer: 'Footer'

libraries:
  - {{ machine_name }}/global